- provide(:title, @user.name)
%div{class: 'row'}
  %aside{class: 'col-md-4'}
    %section{class: 'user_info'}
      %h1
        / gravatar_for is defined in user_helper
        = gravatar_for @user
        = @user.name


<!DOCTYPE html>
<html>
  <head>
    <!-- full title is located in application_helper -->
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/defaults' %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>